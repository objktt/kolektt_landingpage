/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://kolektt.kr', // 실제 배포 도메인으로 변경
  generateRobotsTxt: true, // robots.txt도 자동 생성
  // 필요시 추가 옵션: exclude, changefreq, priority 등
} 